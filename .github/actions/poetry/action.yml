name: poetry

runs:
  shell: bash
  steps:
    - run: pipx install poetry
    - name: Setup python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: "poetry"
    - run: poetry install
